<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen overflow-y-auto bg-zinc-900 p-10 text-white flex flex-col">
        <a class="text-blue-300 mb-10" href="/">Back to Home</a>
        <h1 class="text-3xl font-bold mb-4">All Users</h1>


        <div class="users flex flex-wrap gap-10">

           <% if(users.length > 0){ %>
                <% users.forEach(function(user){ %>

                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                        <div class="h-52 w-full rounded-lg bg-red-700">
                            <img src="<%= user.image %>" class="w-full h-full object-cover" alt="">
                        </div>
        
                        <h3 class="text-xl font-bold mt-4"><%= user.name %></h3>
                        <h5 class="text-zinc-400 mb-5"><%= user.email %></h5>
        
                        <div class="flex gap-3">
                            <a href="/edit/<%= user._id %>" class="text-zinc-500">Edit User</a>
                            <a href="/delete/<%= user._id %>" class="text-red-500">Delete User</a>
                        </div>
                    </div>



                <% }) %> 

           <% } else { %>

                <h2 class="font-bold text-4xl text-blue-300">No Users found!!!</h2>


            <% } %>

         
        </div>
    </div>
    
</body>
</html>