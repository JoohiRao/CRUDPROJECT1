<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-20">
            <a class="text-blue-300 font-bold" href="/read">Read Users</a>
        </div>
        <h1 class="text-3xl font-bold mb-4">Update User</h1>

        <img src="<%= user.image %>" alt="" class="rounded-lg mb-4 w-16 h-16 object-cover">
        <form action="/update/<%= user._id %>" method="post">

            <input type="text" placeholder="name" name="name" class="bg-transparent border-2 border-zinc-800 px-5 py-3" value="<%= user.name %>">
            <input class="bg-transparent border-2 border-zinc-800 rounded-md px-5 py-3" type="text" placeholder="email" name="email" value="<%= user.email %>">
            <input class="bg-transparent border-2 border-zinc-800 rounded-md px-5 py-3" type="text" placeholder="image url" name="image" value="<%= user.image %>">


            <input class="bg-yellow-500 text-white px-5 py-3 rounded-md cursor-pointer ml-3" type="submit" value="Update User">

        </form>
    </div>
    
</body>
</html>